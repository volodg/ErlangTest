calendar:universal_time().
calendar:local_time().
calendar:time_difference()

now_to_datetime(Now)
now_to_local_time(Now)

calendar:time_to_seconds(time()).

{{2011,10,23},{10,40,18}}

http://localhost:8000/report.yaws

http://localhost:8000/echo.yaws?echo=test

http://localhost:8000/test/random_deal.yaws

millisecondsToDate(Milliseconds) ->
   BaseDate      = calendar:datetime_to_gregorian_seconds({{1970,1,1},{0,0,0}}),
   Seconds       = BaseDate + (Milliseconds div 1000),
   { Date,_Time} = calendar:gregorian_seconds_to_datetime(Seconds),
   Date.

////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////

1. Write "self tester" and benchmark

2. Document all

3. Test different issues, errors and timeouts

4. Precess crashes in Supervisor

5. remove Pid when exit or crash from DealerPidAndExpDateByInstrument

6. Send empty reports also ?!

7. Fix all TODO